# Built binary at repo root. Anchored with leading slash so the rule
# doesn't also swallow the cmd/openharness/ source directory or any
# future "openharness" file/directory deeper in the tree.
/openharness

# Local deployment configs (may contain secrets)
config.toml
*.config.toml
mcp.json
oauth-tokens.json
scheduled-tasks.json
